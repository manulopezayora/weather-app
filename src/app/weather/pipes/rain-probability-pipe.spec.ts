import { RainProbabilityPipe } from './rain-probability-pipe';

describe('RainProbabilityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RainProbabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
